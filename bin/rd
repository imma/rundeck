#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "${BLOCK_PATH:-"$shome/work"}/block/script/profile" ~
  source normalize  

  exec java -jar "$shome/vendor/rundeck-cli-${RUNDECK_CLI_VERSION}-all.jar" "$@"

}

source sub "$BASH_SOURCE" "$@"
